<project>
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.linguaphylo</groupId>
        <artifactId>beast-phylonco</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>phylonco-lphybeast</artifactId>
    <!-- phylonco-lphybeast version, it must be consisting with version.xml   -->
    <version>${revision}</version>


    <dependencies>
        <dependency>
            <groupId>io.github.linguaphylo</groupId>
            <artifactId>lphybeast</artifactId>
            <version>${lphybeast.version}</version>
        </dependency>

        <!-- FLC package -->
        <dependency>
            <groupId>io.github.4ment</groupId>
            <artifactId>flc</artifactId>
            <version>${flc.version}</version>
        </dependency>

        <!-- TODO: insert phylonco dependencies here-->
        <!-- phylonco beast -->
        <dependency>
            <groupId>beast2</groupId>
            <artifactId>beast-phylonco</artifactId>
            <version>${revision}</version>
            <scope>system</scope>
            <systemPath>${project.basedir}/lib/phylonco.jar</systemPath>
        </dependency>

        <!-- phylonco lphy -->


    </dependencies>


    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.7.1</version>
                <configuration>
                    <outputDirectory>${project.build.directory}</outputDirectory>
                    <appendAssemblyId>false</appendAssemblyId>
                    <finalName>${project.artifactId}-${project.version}</finalName>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>